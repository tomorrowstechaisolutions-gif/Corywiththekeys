/**
 * Service layer barrel.
 *
 * Domain services (inventory, leads, applications, trade-ins, appointments,
 * customers, messages, partner lots) are added here once the Supabase schema
 * is defined. Pages and route handlers call these instead of querying
 * Supabase directly.
 */
export {};
